# docker-compose build --build-arg now="$(date +%Y-%m-%d)" --build-arg version="1.0.0"

services:
  nexus:
    image: sonatype/nexus3
    volumes:
      - "nexus-data:/nexus-data"
    ports:
      - "8081:8081"

volumes:
  nexus-data: {}
